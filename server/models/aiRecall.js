const mongoose = require('mongoose');

const aiRecallSchema = new mongoose.Schema({
  // Define your AI recall schema fields and types
});

module.exports = mongoose.model('AIRecall', aiRecallSchema);
